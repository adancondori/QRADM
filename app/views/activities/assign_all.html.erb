<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">New Activity</h1>
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="index">Home</a></li>
          <li class="breadcrumb-item active" aria-current="page">New Activity</li>
      </ol>
    </nav>
</div>

<div class="card-block">
    <section class="example">
    <%= form_with(model: @activity, url: "/update_all", method: :post) do |form| %>
      <% if @activity.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@activity.errors.count, "error") %> prohibited this @activity from being saved:</h2>
      
            <ul>
              <% @activity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
            </ul>
          </div>
        <% end %>
            <div class="row">
                <div class="col-md-12">
                    <section class="panel">
                        <div class="panel-body">
                          <div class="form-group">
                            <%= form.label :name, class: 'negrita' %>
                            <div>
                                <%= form.select(:id, @activities.collect {|a| [a.name, a.id]}) %>
                            </div>
                          </div>
                  
                            <br />
                            <br />
                        </div>
                    </section>
                </div>
            </div>
      <% end %>
    </section>
</div>






<%= link_to 'Back', activities_path, class: "btn btn-primary" %>

